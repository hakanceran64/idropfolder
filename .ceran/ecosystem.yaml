# Bu projenin ekosistemden tükettikleri.
# Beyandır — gerçekte ne kurulu olduğunu .ceran/lock.yaml söyler.
# Uygula: dev eco sync   ·   Doğrula: dev eco sync --check
apiVersion: ceran/v2
project:
  name: idropfolder
  profiles: [python]

consume:
  ai-foundation:
    mode: vendor
    version: v1.1.4
  wiki:
    mode: link
